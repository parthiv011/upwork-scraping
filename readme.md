Run: npm install
    - in root directory
    - in frontend

Run: pip i -r requirements.txt
    - in python directory

For backend:
    Run npm start

For frontend:
    Run npm run dev

replace all the env variables
share service-account details